# Postgres connection string (local dev)
# Matches docker-compose credentials (user=postgres password=12345 port=5433)
DATABASE_URL="postgresql://postgres:12345@localhost:5433/autosouq_db?schema=public"
